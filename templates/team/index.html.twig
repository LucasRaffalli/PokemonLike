{% extends 'base.html.twig' %}

{% block title %}Mon Équipe - {{ user.pseudo }}{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-20">
  <div class="max-w-6xl mx-auto mt-8">
    {% for message in app.flashes('success') %}
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
        {{ message }}
      </div>
    {% endfor %}
    {% for message in app.flashes('error') %}
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
        {{ message }}
      </div>
    {% endfor %}

    <div class="flex justify-between items-center mb-8">
      <h1 class="text-4xl font-bold">Mon Équipe ({{ teamCount }}/6)</h1>
      {% if teamCount >= 6 %}
        <button disabled class="bg-gray-400 text-white px-6 py-2 rounded-lg cursor-not-allowed">
          Équipe complète
        </button>
      {% else %}
        <a href="{{ path('app_pokedex') }}" class="bg-primary text-white px-6 py-2 rounded-lg hover:bg-primary-dark transition">
          Ajouter un Pokémon
        </a>
      {% endif %}
    </div>

    <div class="bg-white rounded-lg shadow-lg p-6 mb-8">
      <h2 class="text-2xl font-semibold mb-4">Mes Pokémon</h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6" id="team-pokemon">
        {% if teamPokemons is empty %}
          <div class="text-center py-12 col-span-full">
            <div class="text-6xl mb-4">🎯</div>
            <p class="text-xl text-gray-600 mb-4">Votre équipe est vide</p>
            <p class="text-gray-500 mb-6">Commencez à construire votre équipe en ajoutant des Pokémon depuis le Pokédex !</p>
            <a href="{{ path('app_pokedex') }}" class="inline-block bg-primary text-white px-8 py-3 rounded-lg hover:bg-primary-dark transition">
              Explorer le Pokédex
            </a>
          </div>
        {% else %}
          {% for teamPokemon in teamPokemons %}
            <div class="bg-gray-50 rounded-lg p-4 relative">
              <div class="aspect-square flex items-center justify-center bg-white rounded-lg mb-3">
                <img src="{{ teamPokemon.pokemonImage }}" alt="{{ teamPokemon.pokemonName }}" class="w-full h-full object-contain p-4">
              </div>
              <p class="text-sm text-gray-500 font-semibold">#{{ '%03d'|format(teamPokemon.pokemonId) }}</p>
              <h3 class="text-lg font-bold text-primary mb-3">{{ teamPokemon.pokemonName }}</h3>
              
              <form method="post" action="{{ path('app_team_remove', {id: teamPokemon.id}) }}" onsubmit="return confirm('Voulez-vous vraiment retirer {{ teamPokemon.pokemonName }} de votre équipe ?');">
                <button type="submit" class="w-full bg-red-500 text-white py-2 rounded-lg hover:bg-red-600 transition">
                  Retirer de l'équipe
                </button>
              </form>
            </div>
          {% endfor %}
        {% endif %}
      </div>
    </div>

    <div class="bg-white rounded-lg shadow-lg p-6">
      <h2 class="text-2xl font-semibold mb-4">Règles de l'équipe</h2>
      <ul class="space-y-2 text-gray-700">
        <li class="flex items-center gap-2">
          <span class="text-primary">✓</span>
          <span>Maximum 6 Pokémon par équipe</span>
        </li>
        <li class="flex items-center gap-2">
          <span class="text-primary">✓</span>
          <span>Chaque Pokémon doit être unique dans votre équipe</span>
        </li>
        <li class="flex items-center gap-2">
          <span class="text-primary">✓</span>
          <span>Vous pouvez retirer ou remplacer un Pokémon à tout moment</span>
        </li>
      </ul>
    </div>
  </div>
</div>
{% endblock %}
