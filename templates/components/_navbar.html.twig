<nav class="fixed top-0 left-0 w-full p-4 z-50" role="navigation" aria-label="Navigation principale">
  <div class="mx-auto flex justify-between items-center">
    <a href="{{ path('app_home') }}" class="text-2xl font-bold focus:outline-none focus:ring-4 focus:ring-primary focus:ring-opacity-50" aria-label="Retour à l'accueil">POKEMON LIKE</a>

    <ul class="flex gap-6 lowercase" role="menubar">
    {% if app.user %}
      <li role="none">
        <a href="{{ path('app_dashboard') }}" 
           class="hover:invert transition focus:outline-none focus:ring-4 focus:ring-primary focus:ring-opacity-50" 
           role="menuitem">Dashboard</a>
      </li>
    {% endif %}
    <li role="none">
      <a href="{{ path('app_pokedex') }}" 
         class="hover:invert transition focus:outline-none focus:ring-4 focus:ring-primary focus:ring-opacity-50" 
         role="menuitem">Pokédex</a>
    </li>
    {% if app.user %}
    <li role="none">
      <a href="{{ path('app_team') }}" 
         class="hover:invert transition focus:outline-none focus:ring-4 focus:ring-primary focus:ring-opacity-50" 
         role="menuitem">Mon Équipe</a>
    </li>
    <li role="none">
      <a href="{{ path('app_battle') }}" 
         class="hover:invert transition focus:outline-none focus:ring-4 focus:ring-primary focus:ring-opacity-50" 
         role="menuitem"
         aria-label="Arène de combats">⚔️ Combats</a>
    </li>
    <li role="none">
      <a href="{{ path('app_logout') }}" 
         class="hover:invert transition focus:outline-none focus:ring-4 focus:ring-primary focus:ring-opacity-50" 
         role="menuitem">Déconnexion</a>
    </li>
    {% else %}
      <li role="none">
        <a href="{{ path('app_login') }}" 
           class="hover:invert transition focus:outline-none focus:ring-4 focus:ring-primary focus:ring-opacity-50" 
           role="menuitem">Connexion</a>
      </li>
      <li role="none">
        <a href="{{ path('app_register') }}" 
           class="hover:invert transition focus:outline-none focus:ring-4 focus:ring-primary focus:ring-opacity-50" 
           role="menuitem">Inscription</a>
      </li>
    {% endif %}
    </ul>
  </div>
</nav>
